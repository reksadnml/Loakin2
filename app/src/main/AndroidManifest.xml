<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loakin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".History"></activity>
        <activity android:name=".OrderMasuk" />
        <activity android:name=".CariDriver" />
        <activity
            android:name=".ui.recyclerViewPesanMasuk.PesanMasuk"
            android:theme="@style/LightAppBar" />
        <activity android:name=".Feedback" />
        <activity
            android:name=".bottomNav"
            android:label="@string/title_activity_bottom_nav" />
        <activity android:name=".DaftarDriver" />
        <activity android:name=".DaftarPengguna" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>